'use strict';

define( function () {
    return function () {
        return {
            restrict    : 'EA',
            templateUrl : 'partials/spinner.html',
            link        : function ( scope ) {
                
            }
        };
    };
});